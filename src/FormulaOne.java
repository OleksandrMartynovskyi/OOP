public class FormulaOne extends SportsCar {
    public FormulaOne(String make, String model, int year, String colour, double price, int topSpeed) {
        super(make, model, year, colour, price, topSpeed);
    }
}
